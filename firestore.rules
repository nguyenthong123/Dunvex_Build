rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Ultimate fix for all "Insufficient Permissions" errors for logged-in users
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
